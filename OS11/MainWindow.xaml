<Window x:Class="OS11.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding Main,Source={StaticResource Locator}}"
        Title="Mosaiq - Image Reconstruction" Height="646" Width="925" Padding="0" Margin="0" Opacity="1" MinWidth="904" MinHeight="700" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}" Margin="0" />
		
    </Grid>
	
</Window>
